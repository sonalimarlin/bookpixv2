<!DOCTYPE html>
<html>
<head>
	<title>BookPix</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
	<link rel="stylesheet" href="../stylesheets/main.css">
<script src="https://use.fontawesome.com/7157601a0f.js"></script>
	
</head>
<body>
  <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/"><i class="fa fa-check-square" aria-hidden="true"></i> BookPix</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
									
									<li><a href="/books">All Books</a></li>
									<li><a href="/books/new">Add New Book</a></li>
									<% if(!currentUser){ %>
                    <li><a href="/register"><i class="fa fa-user-plus" aria-hidden="true"></i> Sign Up</a></li>
										<li><a href="/login"><i class="fa fa-sign-in" aria-hidden="true"></i> Login</a></li>
									<% } else { %>
										<li><a href="#" class="logged-in">Logged in as: <%= currentUser.username %></a></li>
                    <li><a href="/logout"><i class="fa fa-sign-out" aria-hidden="true"></i> Logout</a></li>
									<% } %>
									<% if(currentUser && currentUser.isAdmin){ %>
									<li><a href="#">ADMIN USER</a></li>
									<% } %>
                </ul>
            </div>
        </div>
    </nav>
	
	<div class="container">
        
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0){ %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
    </div>
